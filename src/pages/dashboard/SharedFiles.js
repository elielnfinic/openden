import React, {Component} from 'react';
import AppLayout from "../../shared_components/AppLayout";

export default function Dashboard(){
    return (
        <AppLayout title="Shared with you">
            <div>
                All the files shared with you
            </div>
        </AppLayout>
    );
}